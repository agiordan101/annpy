
To do:

	- regularizer
	- EarlyStopping save weights
	- Dropout layer
	- Rename utils.parse -> utils.parsing
	- Create utils.processing with DataProcessing() + shuffle_datasets() + train_val_split() inside
	- raise Exception(...)   ->  raise annpy_error(...).
		To print annpy's error string (+others default data) before Exception string
		OR
		Create my own Execption object ?
	- DataProcessing into annpy